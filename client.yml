eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: clientName
  instance:
    prefer-ip-address: true
spring:
  application:
    name: client_demo
server:
  port: 8686
